<section class="collection-line-section">
  {% for block in section.blocks limit: 1 %}
    {% assign collection = block.settings.collection %}
    {% assign title = block.settings.title | default: collection.title %}
    {% assign url = block.settings.link | default: collection.url %}

    <a class="collection-line" href="{{ url | default: routes.all_products_collection_url }}" {{ block.shopify_attributes }}>
      <span>Collezione {{ block.settings.label }}</span>
      <span class="collection-line-separator">_</span>
      <strong>{{ title }}</strong>
    </a>
  {% endfor %}
</section>

{% schema %}
{
  "name": "Collezioni",
  "settings": [
    { "type": "text", "id": "heading", "label": "Titolo", "default": "Collezione" }
  ],
  "blocks": [
    {
      "type": "collection",
      "name": "Collezione",
      "settings": [
        { "type": "text", "id": "label", "label": "Etichetta", "default": "01" },
        { "type": "collection", "id": "collection", "label": "Collezione Shopify" },
        { "type": "text", "id": "title", "label": "Titolo manuale", "default": "Supereroi" },
        { "type": "textarea", "id": "text", "label": "Testo", "default": "Icone, poteri e simboli ridotti all'essenziale." },
        { "type": "url", "id": "link", "label": "Link manuale" }
      ]
    }
  ],
  "presets": [
    {
      "name": "Collezioni",
      "blocks": [
        {
          "type": "collection",
          "settings": {
            "label": "01",
            "title": "Supereroi",
            "link": "/collections/supereroi"
          }
        }
      ]
    }
  ]
}
{% endschema %}
